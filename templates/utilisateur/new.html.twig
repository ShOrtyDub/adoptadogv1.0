{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <!--        Logo        -->
    <div class="text-center mt-3">
        <img src="{{ asset('assets/img/patte.webp') }}" width="150rem" alt="Logo Adopt a Dog">
    </div>
    <!--        ! Logo        -->

    <h1 class="text-center mt-5 titre">Inscription</h1>

    <div class="text-center matchRech">
        {{ include('utilisateur/_form.html.twig') }}
    </div>

    <div class="text-center mb-5">
        <a class="profil" href="{{ path('app_main') }}">Revenir à l'accueil</a>
    </div>
{% endblock %}
