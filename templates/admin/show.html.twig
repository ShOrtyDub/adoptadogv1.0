{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1 class="text-center">Votre profil</h1>
    <h2 class="text-center">Bonjour {{ admin.prenom }}</h2>

    <div class="flex fiche lignerouge">
        <div class="ligneverte">
            <p><strong>Numero d'admin:</strong> {{ admin.id }}</p>
            <p><strong>Nom:</strong> {{ admin.nom }}</p>
            <p><strong>Prenom:</strong> {{ admin.prenom }}</p>
            <p><strong>Email:</strong> {{ admin.email }}</p>
        </div>
    </div>

    <div class="text-center lignerouge">
        <a href="{{ path('app_admin_index') }}">back to list</a>

        <a href="{{ path('app_admin_edit', {'id': admin.id}) }}">edit</a>

        {{ include('admin/_delete_form.html.twig') }}
    </div>
{% endblock %}
