{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <main>
        <h1 class="text-center sizeh1">Adopt a Dog</h1>

        <div class="container text-center lignerouge">
            <h2>Bienvenu sur Adopt a Dog!</h2>
            <p>
                L'application de notre refuge où vous pouvez voir les chiens en adoption.
                Et peut-être, qui sait, leur permettre de se trouver un foyer...
            </p>

            {% if app.user %}
                <a href="{{ path('app_chien_index', {'id': app.user.id}) }}">Voir les chiens</a>
            {% else %}
                <a href="{{ path('app_chien_index') }}">Voir les chiens</a>
            {% endif %}
        </div>

        <div>
            <a class="nav-link" href="{{ path('app_admin_index') }}">Voir les admins</a>

            <a class="nav-link" href="{{ path('app_utilisateur_index') }}">Voir les utilisateurs</a>

        </div>
    </main>

{% endblock %}
